# 待办事项 0614

* 保存结构图的手动拖拽位置- （正在调试）
* 结构图的筛选模式，进一步优化细化

* 代码 架构解读 相关的 agent功能 
  * 组件架构 标签模式 可选 选定后 执行分析
  * 组件架构 结构图模式 增加 选择模式（或右键菜单 单选）， 选定节点， 解析
* web文档浏览 交互 标注 等web端功能。

* 对接三方agent的 mcp功能
* 文档生成 设计方案生成 测试用例 设计规约 软件工程相关内容生成
* 对接三方agent 驱动开发任务
* 架构分析评审 需求分析评审 代码质量分析评审。
* 软件知识库规范化生成
* topocode-cloud 设计开发
* mermaid plantuml 生成工具 tools 
* 架构跟踪 架构比对功能 架构快照 版本管理

## 小优化事项


## 组件、架构、分析需求细化

* 组件架构 机构图模式下，  跟随模式、独立模式 从图配置中移出， 在工具条单独提供模式切换选项。
* 力导向图、热力图、表格， 切换为下拉列表 三选一， 节省空间 给 模式切换 选项。
* 力导向图、热力图，缩放按钮 UI统一（ 缩放、重置、热力图切花内尺寸）， 位置都放在画布的左上， 保持适当距离 不要顶死。
* 表格、热力图， 增加AI助手 气泡。
* AI助手 气泡， 在右面版 AI助手打开时 隐藏， 右面板关闭时 显示。 去掉 气泡的默认引导语句。

问题反馈：
* 热力图 整图无法拖动， 原来是可以拖动的。
* 热力图 矩阵尺寸不可选，原来是可选的。
* 全屏状态下 AI助手气泡 取消，  因为底部工具栏已经有了AI助手呼出按钮。
* 全屏状态下 底部工具栏 有两个退出全屏按钮， 去掉 推出全屏 文字UI按钮
* 下拉列表选项，在全屏模式下， 应该向上展开。
* 全屏模式下 没有 跟随模式、独立模式的选项， 增加选项，且全屏模式下列表向上展开。

* 组件架构 标签模式， AI 引导探索 气泡显示 ,在右面版 AI助手打开时 隐藏， 右面板关闭时 显示.
* 热力图 整图无法拖动， 原来是可以拖动的。
* 热力图 矩阵尺寸不可选，原来是可选的。
