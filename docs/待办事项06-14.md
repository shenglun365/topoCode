# 待办事项 0614

* 代码 架构解读 相关的 agent功能 
  * 组件架构 标签模式 可选 选定后 执行分析
  * 组件架构 结构图模式 增加 选择模式（或右键菜单 单选）， 选定节点， 解析
* web文档浏览 交互 标注 等web端功能。 标注 备注 功能确实可以完善。 还可以考虑增加LLM对话能力

* 对接三方agent的 mcp功能
* 文档生成 设计方案生成 测试用例 设计规约 软件工程相关内容生成
* 对接三方agent 驱动开发任务
* 架构分析评审 需求分析评审 代码质量分析评审。

* 软件知识库规范化生成
* topocode-cloud 设计开发
* mermaid plantuml 生成工具 tools 
* 架构跟踪 架构比对功能 架构快照 版本管理
  * 架构变更感知
* topocode-cloud 
  * API摘要服务 摘要缓存 。
  * 项目分析结果的整体打包下载安装。

* agent 对话模式 上下文管理 项目语义引用。

* 文档web端 纯静态资源导出

## 问题及优化事项



## agent功能 细化实现


- agent 代码讲解 代码问答能力

做什么 提取功能
怎么做 提取算法 数据结构 调用流程
为什么这么做，提取背景分析 横向纵向对比
适用于什么场景
场景描述的纬度 业务约束 软件约束 硬件约束 环境兼容 性能限制
提取流程 提取数据结构 提取算法 时序图 用例图 数据流图
web端，文档备注批注 修改模式。  是否需要增加AI对话？
应用内的 AI助手 需要限制对话的目的范围，  自由对话考虑转移到web端。

整理分析流程

AST解析， 项目摘要， 文件摘要， 组件 L0~L5分析， 整体架构摘要

对话问答 对话问答的范围 上下文管理 RAG增强功能。


v 0.2.0 beta 上线之前 需要完善的任务列表：
1、本期不提供MCP相关的功能，先关闭。
2、不提供任务快照 项目快照能力， 先关闭。
6、结构图优化， 节点颜色锁定，刷新不随机更换，避免用户认知混乱。

3、结构分析的 导入导出功能。
  3.1、先说导出，按项目+任务模式导出，一个项目一个包， 可以导出全部任务，也可以选择导出单个或某几个任务。
    导出文件哈希（不知道当前是哈希校验 还是修改时间校验），AST解析的所有节点， 社区结构， 文档结构
    分类型导出为jsonl格式。
  3.2、再说导入（不是当前的源码导入）， 可以新建空项目导入结构和文档， 也可以选择已导入源码的项目，导入结构和文档。
    已存在的项目，导入时要校验是否匹配，给出匹配率的提示， 文件匹配率低代表导入的结构分析和源码差异大， 不强制限制导入，由用户自主选择。 导入时还需校验任务ID是否冲突， 项目ID可以关联或映射、替换为导入环境的项目ID。
    导入成功后，在新项目或目标项目下生成对应的任务，和任务对应的结构分析内容。可以进行问答引用和web浏览。

4、用户注册，用户登录，资源下载 资源导入。
5、web端 AI对话能力
  对话 agent 画图uml mermaid
  对话导入文档
  组件库 组件规格库 算法库 数据结构库 代码段库
  生成 设计 实现 测试 修正 打包 发布  
6、应用内部agent对话 范围限定， topocode使用相关， 相当于帮助手册。

 
4、用户注册，用户登录，资源下载 资源导入。
  用户系统 待测试
5、web端 AI对话能力
  待完善 待测试
6、应用内部agent对话 范围限定， topocode使用相关， 相当于帮助手册。
  待完善 待测试
7、流水线、 指令系统、 harness、 skills、 tools 的 自定义 拓展能力


“本地应用 (topoOne-ui, git 管理)”
这里的 git管理 是做什么用的。

“纯前端，无后端认证逻辑”
后端是不是需要代理http/https请求？ 避免跨域等问题。

后端服务，需要增加邮箱验证码， 手机验证码的功能接口， 还要有防刷机制。
前端需要增加 邮箱验证码， 手机验证码 的配套UI。


